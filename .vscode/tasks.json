{
  "version": "2.0.0",
  "tasks": [
    {
      "command": "pnpm dev",
      "options": { "cwd": "${workspaceFolder}/app" },
      "label": "Run App",
      "type": "shell",
      "runOptions": { "runOn": "folderOpen" }
    }
  ]
}
